​   Idea is to process both the string until we reached '.' or '\0'
  
   then we compare both the number and output the result 
   
   if version 1 > version 2
       return 1
    
   if version 1 < version2
       return -1
  
  
